---
name: "listen"
category: "event"
type: "event"
language: "LSL"
description: "Fires when a chat message matching an active llListen filter is received"
wiki_url: "https://wiki.secondlife.com/wiki/Listen"
first_fetched: "2026-03-09"
last_updated: "2026-03-09"
signature: "listen(integer channel, string name, key id, string message)"
parameters:
  - name: "channel"
    type: "integer"
    description: "Channel the message was received on"
  - name: "name"
    type: "string"
    description: "Prim name or avatar legacy name of the speaker"
  - name: "id"
    type: "key"
    description: "UUID of the speaking avatar or prim"
  - name: "message"
    type: "string"
    description: "The text that was spoken"
deprecated: "false"
---

# listen

```lsl
listen(integer channel, string name, key id, string message)
{
    // handle incoming message
}
```

Triggered when a chat message matches the filters of an active `llListen` registration.

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `channel` | integer | Channel the message arrived on |
| `name` | string | Speaker's prim name or avatar legacy name |
| `id` | key | Speaker's UUID |
| `message` | string | The spoken text |

## Caveats

- Fires only for messages matching ALL active filters of a registered `llListen`.
- State changes and script resets automatically remove all active listeners.
- A prim cannot hear its own chat.
- The **listen location** is the root prim's position, not the listening child prim's position. Exception: this does not apply to chat generated by avatars sitting on the same object as the listening prim.
- If a message satisfies multiple `llListen` registrations, only one `listen` event fires.
- When ownership changes, listeners registered with a previous owner's key do not update automatically.

## Example

```lsl
integer listenHandle;

default
{
    state_entry()
    {
        listenHandle = llListen(0, "", llGetOwner(), "");
    }

    listen(integer channel, string name, key id, string message)
    {
        llOwnerSay(name + " said: " + message);
        llListenRemove(listenHandle);
    }

    changed(integer mask)
    {
        if (mask & CHANGED_OWNER)
            llResetScript();
    }
}
```

## See Also

- `llListen` — register a listener
- `llListenRemove` — remove a listener
- `llListenControl` — enable/disable a listener
- `llSay`, `llShout`, `llWhisper`, `llRegionSay` — transmit chat


<!-- wiki-source -->
_Source: [SL Wiki](https://wiki.secondlife.com/wiki/listen) — scraped 2026-03-18_

## Caveats

- On state change or script reset all listens are closed automatically.
- When an object changes owner any listen registered with llGetOwner will not automatically update itself until the script is reset. The scripter can catch this scenario per the example below.
- If a message satisfies the filters of multiple llListens registered by the script, only one event will be raised.
- A prim cannot hear/listen to chat it generates.
- The location of the listen is not at the listening prim's location but at the root prim's location. This is to deter people using child prims for spying over parcel boundaries. Chat generating functions on the other hand generate chat at the calling prim's location (and not at the root prim's location).
- The above is not true for chat generated by avatars sitting on the same object as the listening prim.

## Examples

|  | Important: Please make sure that you close open listeners where possible. You'll make the Second Life experience so much better when paying attention to details here. |
| --- | --- |

Trivial example to listen to any chat from the object owner and respond once. To reduce lag and avoid spamming surrounding users, it is vastly preferable to listen on channels other than 0 and to trigger the listen event by chatting on an alternative channel such as '/5 hello'.

```lsl
// says beep to owner the first time owner says something in main chat;
integer listen_handle;

default
{
    state_entry()
    {   //Registers the listen to the owner of the object at the moment of the call. This does not automatically update when the owner changes.
        // Change 0 to another positive number to listen for '/5 hello' style of chat.
        listen_handle = llListen(0, "", llGetOwner(), "");
    }

    listen( integer channel, string name, key id, string message )
    {
        llOwnerSay("beep");
        // Stop listening until script is reset
        llListenRemove(listen_handle);
    }

    changed(integer mask)
    {   //Triggered when the object containing this script changes owner.
        if(mask & CHANGED_OWNER)
        {
            llResetScript();   // This will ensure the script listens to the new owner, and doesn't continue listening to the creator.
        }
    }
}
```

## See Also

### Functions

- llListen
- llListenControl
- llListenRemove
- llDialog
- **llOwnerSay** — PUBLIC_CHANNEL
- **llWhisper** — Sends chat limited to 10 meters
- **llSay** — Sends chat limited to 20 meters
- **llShout** — Sends chat limited to 100 meters
- **llRegionSay** — Sends chat limited to region

<!-- /wiki-source -->
